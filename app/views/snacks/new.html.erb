<% content_for :title, "おやつを記録 — おやつ貯金" %>

<div class="app-shell">
  <header class="topbar">
    <%= link_to "← ホームへ", root_path, class: "back-link" %>
    <h1>おやつを記録する 🎀</h1>
  </header>

  <main class="content">
    <%= render "shared/flash" %>
    <section class="card">
      <%= render "form" %>
    </section>
  </main>

  <%= render "shared/bottom_nav", active: :record %>
</div>
